# Argumentaion translator

Small script to translate between two syntax formats for argumentation frameworks


run:
```python translate.py *path_to_apx_file* **name_of_file_to_save_to**```

where `**` is optional

# TO DO
* Translation backwards
